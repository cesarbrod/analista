Usei esse prompt no Google Antigrivity para transformar a aplicação que roda no terminal para uma em flask. O prompt usado foi o seguinte:

Convert the CLI analista.py into a Flask web application suitable for deployment on PythonAnywhere.
